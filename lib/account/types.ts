export type OrderProduct = {
  id: string;
  name: string;
  price: string;
  color: string;
  material: string;
  size: string;
};

export type CaseItem = {
  id: string;
  title: string;
  roomType: string;
  description: string;
  likes: number;
  messages: number;
};
